import { command as autoreply } from './autoreply'
import { command as channel } from './channel'
import { command as ping } from './ping'

export const commands = [
  autoreply,
  ping,
  channel
]
